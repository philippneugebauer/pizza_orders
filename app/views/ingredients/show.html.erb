<h1> View Ingredients
  <%= link_to "Edit", edit_ingredient_path(@ingredient), class: "btn btn-outline-warning" %> |
  <%= button_to "Delete", @ingredient, method: :delete, class: "btn btn-outline-danger" %>
  <span class="float-right">
    <%= link_to "Back to ingredients", ingredients_path, class: "btn btn-outline-secondary" %>
  </span>
</h1>

<div class="table-responsive">
  <table class="table mt-3" aria-label="table for Ingredients">
    <tr>
      <td>Name</td>
      <td><%= @ingredient.name %></td>
    </tr>
    <tr>
      <td>Price</td>
      <td><%= @ingredient.price %></td>
    </tr>
  </table>
</div>
